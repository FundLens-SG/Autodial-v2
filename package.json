{
  "name": "autodial-v2",
  "version": "0.0.0",
  "private": true,
  "description": "AutoDial v2 static PWA workspace",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node tests/smoke.test.mjs",
    "validate": "node tools/validate-autodial.js && node tests/smoke.test.mjs"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^7.0.0"
  }
}
